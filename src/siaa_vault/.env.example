APP_NAME=siaa-vault
APP_ENV=development
APP_PORT=8001

# ⚠️ MASTER KEY — gerada UMA vez e nunca mais muda
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Se mudar, todos os segredos no banco ficam ILEGÍVEIS
MASTER_KEY=gere-e-cole-aqui-nunca-commite-isso

# JWT para sessões do Vault
# Gere com: openssl rand -hex 32
JWT_SECRET=outro-segredo-forte-aqui
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=15       # tokens curtos — abre sessão, usa, fecha

# Admin — acesso para criar clientes e gerenciar o vault
ADMIN_PASSWORD=senha-forte-do-admin

# SQLite
DATABASE_URL=sqlite+aiosqlite:///./siaa_vault.db

# Módulos permitidos (comunicação interna)
# Nome dos clientes que podem se registrar no vault
ALLOWED_CLIENTS=siaa-bot,siaa-proxy,siaa-scraper
