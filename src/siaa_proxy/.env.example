APP_NAME=siaa-proxy
APP_ENV=development
APP_PORT=8000

# Gere com: openssl rand -hex 32
SECRET_KEY=troque-por-uma-chave-segura-em-producao
API_TOKEN_EXPIRE_DAYS=30

# SQLite
DATABASE_URL=sqlite+aiosqlite:///./siaa_proxy.db

# Intervalos dos cron jobs
CRON_FETCH_PROXIES_MINUTES=60      # busca novas listas a cada 1h
CRON_VALIDATE_PROXIES_MINUTES=15   # valida proxies a cada 15min

# Validação
PROXY_TIMEOUT_SECONDS=10
PROXY_TEST_URL=https://httpbin.org/ip
MAX_CONCURRENT_VALIDATIONS=30

# Playwright (navegador serverless)
BROWSER_TIMEOUT_MS=30000
BROWSER_HEADLESS=true
