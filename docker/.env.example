# =============================================================
# SIAA — Variáveis de Ambiente
# Copie para .env e preencha os valores
# O .env NUNCA deve ser commitado no git
# =============================================================

# -------------------------------------------------------------
# Telegram
# -------------------------------------------------------------
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
SESSION_TIMEOUT=300

# -------------------------------------------------------------
# Bot
# -------------------------------------------------------------
BOT_NAME=Siaa
USER_NAME=

# -------------------------------------------------------------
# Localização (Rio de Janeiro)
# -------------------------------------------------------------
LATITUDE=-22.9035
LONGITUDE=-43.2096

# -------------------------------------------------------------
# Ollama — LLM local (roda no mesmo compose)
#
# Com 24GB de RAM do Free Tier, roda com folga.
# Modelos recomendados por RAM/qualidade:
#   granite3.3:2b  → ~2GB RAM  — rápido, bom para comandos
#   llama3.2:3b    → ~2.5GB    — boa conversa em PT
#   llama3.1:8b    → ~6GB      — melhor qualidade, mais lento
# -------------------------------------------------------------
OLLAMA_URL=http://ollama:11434/api/generate
OLLAMA_MODEL_FAST=granite3.3:2b
OLLAMA_MODEL_CHAT=granite3.3:2b

# -------------------------------------------------------------
# Volume de dados
# Docker:  /siaa-data  (montado pelo docker-compose)
# Local:   volumes/siaa-data
# -------------------------------------------------------------
SIAA_DATA_DIR=/siaa-data

# -------------------------------------------------------------
# SVM — Treinamento
# true = força retreinamento no próximo boot do container
# false = usa modelo existente (padrão)
# -------------------------------------------------------------
FORCE_TRAIN=false

# -------------------------------------------------------------
# Whisper (transcrição de áudio)
# Desabilitado por padrão no Free Tier (sem RAM suficiente)
# base | small | medium (quanto maior, mais RAM consome)
# -------------------------------------------------------------
WHISPER_MODEL_SIZE=base

# -------------------------------------------------------------
# Módulos externos (deixe vazio se não configurado)
# -------------------------------------------------------------
GMAIL_CREDENTIALS_PATH=

PROXY_SERVER_URL=
PROXY_API_TOKEN=

VAULT_SERVER_URL=
VAULT_CLIENT_ID=siaa-bot
VAULT_CLIENT_SECRET=
