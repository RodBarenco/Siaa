# =============================================================
# SIAA — Variáveis de Ambiente
# Copie para .env e preencha os valores
# O .env NUNCA deve ser commitado no git
# =============================================================

# -------------------------------------------------------------
# Telegram
# -------------------------------------------------------------
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
SESSION_TIMEOUT=300

# -------------------------------------------------------------
# Bot
# -------------------------------------------------------------
BOT_NAME=Siaa
USER_NAME=

# -------------------------------------------------------------
# Localização (Rio de Janeiro)
# -------------------------------------------------------------
LATITUDE=-22.9035
LONGITUDE=-43.2096

# -------------------------------------------------------------
# Ollama (LLM local)
# Docker:  http://ollama:11434/api/generate
# Local:   http://localhost:11434/api/generate
# -------------------------------------------------------------
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL_FAST=granite3.3:2b   # classificação e extração rápida
OLLAMA_MODEL_CHAT=granite3.3:2b   # chat e raciocínio (usar modelo mais robusto caso necessário)

# -------------------------------------------------------------
# Volume de dados
# Docker:  /siaa-data  (volume montado pelo docker-compose)
# Local:   volumes/siaa-data  (relativo à raiz do projeto)
# -------------------------------------------------------------
SIAA_DATA_DIR=volumes/siaa-data

# -------------------------------------------------------------
# Módulos externos (deixe vazio se ainda não configurado)
# -------------------------------------------------------------
GMAIL_CREDENTIALS_PATH=

# Siaa-Proxy (quando o módulo web estiver ativo - sempre o msm valor nas 2 chaves)
PROXY_SERVER_URL=http://localhost:8001
PROXY_SECRET_KEY=
SECRET_KEY=
# Siaa-Vault (quando o módulo de credenciais estiver ativo)
VAULT_SERVER_URL=http://localhost:8002
VAULT_CLIENT_ID=siaa-bot
VAULT_CLIENT_SECRET=

