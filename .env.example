# =============================================================
# SIAA — Variáveis de Ambiente
# Copie para .env e preencha os valores
# O .env NUNCA deve ser commitado no git
# =============================================================

# -------------------------------------------------------------
# Telegram
# -------------------------------------------------------------
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
SESSION_TIMEOUT=300

# -------------------------------------------------------------
# Bot
# -------------------------------------------------------------
BOT_NAME=Siaa
USER_NAME=

# -------------------------------------------------------------
# Localização (Rio de Janeiro)
# -------------------------------------------------------------
LATITUDE=-22.9035
LONGITUDE=-43.2096

# -------------------------------------------------------------
# Ollama (LLM local)
# Docker:  http://siaa-ollama:11434  (nome do container)
# Local:   http://localhost:11434
# -------------------------------------------------------------
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL_FAST=granite3.3:2b
OLLAMA_MODEL_CHAT=granite3.3:2b

# -------------------------------------------------------------
# Volume de dados
# Docker:  /siaa-data  (volume montado pelo docker-compose)
# Local:   volumes/siaa-data
# -------------------------------------------------------------
SIAA_DATA_DIR=volumes/siaa-data

# -------------------------------------------------------------
# Siaa-Proxy
# Docker:  http://siaa-proxy:8001
# Local:   http://localhost:8001
# PROXY_SECRET_KEY e SECRET_KEY devem ter o mesmo valor
# -------------------------------------------------------------
PROXY_SERVER_URL=http://localhost:8001
PROXY_SECRET_KEY=
SECRET_KEY=

# -------------------------------------------------------------
# Siaa-Vault — KV store cifrado por módulo
# Docker:  http://siaa-vault:8002
# Local:   http://localhost:8002
#
# MASTER_KEY: chave Fernet — gere UMA VEZ e nunca mude
#   python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# JWT_SECRET: assina os tokens de sessão dos módulos
#   openssl rand -hex 32
#
# INTERNAL_SECRET_KEY: chave fixa que os containers usam para
#   buscar o token rotativo via GET /internal/current-token
#   openssl rand -hex 32
#
# ADMIN_PASSWORD: acesso às rotas /admin/* do vault
#   openssl rand -hex 16
#
# VAULT_CLIENT_ID / VAULT_CLIENT_SECRET: credenciais do siaa-bot
#   registradas no vault via: make vault-register ID=siaa-bot NS='*'
# -------------------------------------------------------------
VAULT_SERVER_URL=http://localhost:8002
MASTER_KEY=
JWT_SECRET=
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=15
INTERNAL_SECRET_KEY=
TOKEN_ROTATE_HOURS=1
ADMIN_PASSWORD=
VAULT_CLIENT_ID=siaa-bot
VAULT_CLIENT_SECRET=

# -------------------------------------------------------------
# Módulos externos (deixe vazio se ainda não configurado)
# -------------------------------------------------------------
GMAIL_CREDENTIALS_PATH=
